# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/

# IDE
.idea/
.vscode/

# ML artifacts / large binaries
*.h5
*.pth
*.ckpt
runs/
checkpoints/*
!checkpoints/best_model.pth
!checkpoints/soft_model.pth
!checkpoints/soft_model_mt.pth
!checkpoints/joint_model.pth
dataset/latest/
# PCA-проекция фрагментов (4.9 КБ): без неё freeze_pca() не вызывается
# и проекция остаётся обучаемой при дообучении с чистого клона
!dataset/pca_components.pth

# Downloaded external datasets (MotifBench outputs and evaluations)
data/ood/

# Кластеры последовательностей RCSB (22 МБ, скачиваются dataset/sequence_clusters.py)
dataset/clusters-by-entity-30.txt

# Generated dataset manifests (reproducible via dataset/make_split.py, seed=42)
dataset/manifest_v1.csv
dataset/manifest_v1_split*.csv

# Data logs
*.log

# Прогоны evaluation/ и analysis/ (воспроизводимы запуском этих же скриптов).
# Сам каталог остаётся в репозитории: скрипты пишут в него без mkdir.
results/*
!results/.gitkeep

# По-остаточные метки CA-lDDT (воспроизводимы через training/build_lddt_labels.py)
dataset/lddt_labels/

# LaTeX build artifacts
*.aux
*.out
*.toc
*.synctex.gz
course_work*.pdf

# Coursework texts (kept locally only)
course_work*.tex

# Логи полного прогона
logs/

# Прогоны МД: траектории и подготовка систем (воспроизводимы через md/prepare.sh + md/produce.sh)
md/runs/
md/bench/
